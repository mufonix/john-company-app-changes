// src/redux/reducers/playerReducer.js

const initialState = {
    // Define initial state related to players
  };
  
  const playerReducer = (state = initialState, action) => {
    switch (action.type) {
      // Handle actions
      default:
        return state;
    }
  };
  
  export default playerReducer;
  